import React from 'react';

const Landing = () => {
    return(
        <div style={{textAlign: "center"}}>
            <h1>
                Feedback App
            </h1>
            Collect feedback form for your users
        </div>
    )
};

export default Landing;